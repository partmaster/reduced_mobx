name: reduced_mobx
version: 0.4.0
description: Implementation of the state management API 'reduced' on top of the state management framework 'MobX'.
homepage: https://github.com/partmaster/reduced_mobx
repository: https://github.com/partmaster/reduced_mobx.git
publish_to: none

environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  reduced: 0.4.0
#  reduced: 
#    git:
#      url: https://github.com/partmaster/reduced.git
#      ref: v0.4.0
  flutter_mobx: ^2.0.6+5
  mobx: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  mobx_codegen: ^2.1.1
